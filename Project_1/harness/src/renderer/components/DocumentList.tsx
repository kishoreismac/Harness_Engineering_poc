import type { DocumentRecord } from '../../shared/types';
export function DocumentList({ documents, selectedId, onSelect }: { documents: DocumentRecord[]; selectedId: string | null; onSelect(id: string): void }) { return <div className="document-list">{documents.length === 0 ? <div className="empty"><strong>Your library is empty</strong><span>Import a text or Markdown file to begin.</span></div> : documents.map((document) => <button className={`document-card ${selectedId === document.id ? 'selected' : ''}`} key={document.id} onClick={() => onSelect(document.id)}><span className="file-icon">{document.filename.endsWith('.md') ? 'MD' : 'TXT'}</span><span><strong>{document.title}</strong><small>{formatBytes(document.size)} · {document.status}</small></span></button>)}</div>; }
function formatBytes(bytes: number): string { return bytes < 1024 ? `${bytes} B` : `${(bytes / 1024).toFixed(1)} KB`; }
