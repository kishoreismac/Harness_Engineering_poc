import { useState } from 'react';
export function QuestionPanel({ onAsk, busy }: { onAsk(question: string): Promise<void>; busy: boolean }) { const [question, setQuestion] = useState(''); const submit = async () => { if (!question.trim() || busy) return; const value = question; setQuestion(''); await onAsk(value); }; return <div className="question-panel"><textarea aria-label="Question" placeholder="Ask a question about your library…" value={question} onChange={(event) => setQuestion(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); void submit(); } }} /><button disabled={!question.trim() || busy} onClick={() => void submit()}>{busy ? 'Thinking…' : 'Ask'}</button></div>; }
